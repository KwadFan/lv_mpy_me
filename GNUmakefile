ESPIDF ?= /buildroot/esp-idf
BOARD ?= GENERIC
#PORT ?= /dev/ttyUSB0
#FLASH_MODE ?= qio
#FLASH_MODE ?= dio		# Uncomment for ESP-WROOM-32
#FLASH_SIZE ?= 4MB
#CROSS_COMPILE ?= xtensa-esp32-elf-

include Makefile